{
  "name": "torn-overseer-scripts",
  "version": "0.22.0",
  "private": true,
  "description": "Torn Overseer userscripts",
  "scripts": {
    "test": "node --test test/*.test.js",
    "check": "node --check Torn-Overseer-Chain-Watch.user.js && npm test --silent"
  },
  "devDependencies": {
    "jsdom": "^29.1.1"
  }
}
